Now i want to start development for my device management module. Pls refer to "device" table structure in genvolt_database_scripts.sql file under "D:\GenVoltRashmiRepo12Oct\documents" and create plan document for device management module.The module will include actions list,create,edit,delete operations for a device. pls follow the similar document structure as the othe plan documents in same folder. i do not need coding details in my planning documet. also pls remove any reference for iot_data_sick and iot_data_new. ill work on intgrating these tables at a later stage.Keep the coding standard same as existing code base. Please do not start any implementation, ill review the document first and then move forward. 

in validaion rule section the client_id is not a required field but yes must exist in client table. This field should be dropdown and it will come from client table. Also we are not sure about 'Onboard Channel Device' permission for channel configuration under "Permission Middleware" so pls remove any reference of this from the document and replace "Onboard Device" with "Transfer Device" permission from permissions table and to Edit device use "Edit Device" permission from permissions table.

Add Device button would only be visible to users who will have "Transfer Device" permission : Genvolt the SYSTEM_ADMIN
NOTE ~ Transfer Device permission would only be assigned to CLIENT_ADMIN

prompt : logged-in user has client id mapped and On Device Management, client dropdown on "Add New Device" UI & "Transfer Device" UI should show all the clients below its hierarchy. example all the clients to which logged user client is parent in hierachy to the top level

prompt: when device is transferred from one clien to another then seller_id and buyer_id and device_id record should be created in client_device table and client_id columm of device table should be updated to client id to whihc device is transferred. example if genvolt tranfer ABS0111 device to Acme Corporation then seller_id id would be genvolt id and buyer_id would be Acme Corporation and client_id columm of device table should be updated by Acme Corporation id.
  
promt: if transfer device from one client to another in same hierachy abd error message if it is transferred below hierachy
if device is transferred to one client and this client has not transferred it to its child client, then this device should be able to transfer to another client if they are on same level. example Genvolt transferred ABC022 device to Acme corporation and Acme corporation has not assigned this to its client further down its hierachy then device ABC022 can be transferred from Acme corporation to Tech flow industries. and while doing this client id in device table should be updated with client of newly assigned client in this case Tech flow industries and buyer_id of client_device table should also be updated with newly assigned client in this case Tech flow industries.
Also if Acme corporation transferred this device to its client AC1 Corp and if user try to transfer ABC022 from Acme corporation to Tech flow industries then system should give error message "This Device can not be transferred to another client since its already been assigned to another client" and should not allow this transfer.

prompt: if device is assigned to AC1 Corp and Genvolt is assigning device to AC1 Child Corp  then client_device table should be updated by seller_id AC1 Corp and buyer_id AC1 Child Corp (currently client_id column in device table) and client_id device table should be updated by AC1 Child Corp

Consider a scenario where a device S1S1 is assigned to a client "AC1 Corp" and genvolt user(up in hierarchy) with "Transfer Divice" permission logs-in, and try to assign it to Ac2 Corp. Implement a logic by looking into device table where its currenly assigned to i.e. AC1 Corp and update transfer history for this device by updating buyer_id by "AC2 Corp" and seller_id id with parent of "AC2 Corp" and update client_id in device table by "AC2 Corp".
Another similar example would be, suppose device D1D1 is assigned to "AC1 Child Corp"(child of AC1 Corp) and user of "Acme Corporation" with "Transfer Device" permission logs-in and assign it to "AC2 Child Corp" (child of AC1 Corp). in this case in look into device table where its currenly assigned to i.e. "AC1 Child Corp" and update transfer history for this device by updating buyer_id by "AC2 Child Corp" and seller_id id with parent of "AC2 Child Corp" and update client_id in device table by "AC2 Child Corp". look into code and let me know what changes are required to implement this logic

my scenario is, device is assigned to "AC1 Corp" which is child of "Acme Corporation" and in client_device table seller_id for this is "Acme Corporation" and buyer id is "AC1 Corp" and now Genvolt user transfer this from "AC1 Corp" to "AC2 Corp" then let me know what values would be updated in client_device table for seller_id & buyer_id?
  
prompt: if device is transferred below hierachy then ask him to add records into client_device and update client_id into device table

on Device Management while Adding new device or Transferring device, consider a scenario where a device XX1 is with Genvolt and hierarchy from parent to child is Genvolt -> Acme Corporation -> AC1 Corp. Now of Genvolt try to assign this device to "AC1 Corp" directly then implement a logic to create tranfer history chain as seller_id of Genvolt to buyer_id of Acme Corporation and another one seller_id of "Acme Corporation" to buyer_id of "AC1 Corp" and update client_id in device table by "AC1 Corp". Another similar example would be suppose FF1 device is with Genvolt and hierarchy from parent to child is Genvolt -> Acme Corporation -> AC1 Corp -> AC1 Child Corp. Now of Genvolt try to assign this device to "AC1 Child Corp" directly then in this case tranfer history chain would be as follow 1. seller_id Genvolt to buyer_id Acme Corporation 2. seller_id Acme Corporation to buyer_id AC1 Corp 3. seller_id AC1 Corp to buyer_id AC1 Child Corp and update client_id in device table by "AC1 Child Corp". 

on Device Management while Adding new device or Transferring device, consider following parent child hierarchy Genvolt -> Acme Corporation -> AC1 Corp-> AC1 Child Corp and now consider 2 scenarios. 1. Genvolt user with "Transfer Device" permission transfer this device directly to "AC1 Child Corp" and do not follow hierarchy chain i.e. parent to child 2. Acme Corporation user with "Transfer Device" permission transfer this device directly to "AC1 Child Corp" and do not follow hierarchy chain i.e. parent to child. In such scenarios system should check if transfer history does not exits from Genvolt(seller_id) to Acme Corporation(buyer_id), Acme Corporation(seller_id) to AC1 Corp(buyer_id) and AC1 Corp(seller_id) to AC1 Child Corp(buyer_id) similarly for case 2 Acme Corporation(seller_id) to AC1 Corp(buyer_id) and AC1 Corp(seller_id) to AC1 Child Corp(buyer_id) then it should create transfer history and then update client_id of AC2 Child Corp to device table. pls look into code and let me know what changes you would make for this and previous logic should not break.

 scenario where a device XX1 is with Genvolt and hierarchy from parent to child is Genvolt -> Acme Corporation -> AC1 Corp. Now of Genvolt try to assign this device to "AC1 Corp" directly then implement a logic to create tranfer history chain as seller_id of Genvolt to buyer_id of Acme Corporation and another one seller_id of "Acme Corporation" to buyer_id of "AC1 Corp" and update client_id in device table by "AC1 Corp". Another similar example would be suppose FF1 device is with Genvolt and hierarchy from parent to child is Genvolt -> Acme Corporation -> AC1 Corp -> AC1 Child Corp. Now of Genvolt try to assign this device to "AC1 Child Corp" directly then in this case tranfer history chain would be as follow 1. seller_id Genvolt to buyer_id Acme Corporation 2. seller_id Acme Corporation to buyer_id AC1 Corp 3. seller_id AC1 Corp to buyer_id AC1 Child Corp and update client_id in device table by "AC1 Child Corp". 

Genvolt(HK00012)							HK00099 
	Acme Corporation(HK00012)				HKMI (HK00099)	
		AC1 Corp(HK00012)  						Railways 1(HK00099)
			AC1 Child Corp (HK00012)
			AC2 Child Corp (HK00012)
		AC2 Corp
	Tech flow industries(3)
		TF1 Corp
		TF2 Corp
User
	Acme Corporation(2)	
		AcmeUser1		AcmeUser1/test@123
		AcmeUser2
			AC1 Corp
				ACCorpUser1		ACCorpUser1/test@123
					AC1ChildCorpUser1	
	Tech flow industries(3) TechflowUser1
34: GenVolt -> Acme
		Acme -> AC1
		
on User Management, while adding new user the client dropdown should only show clients which are below logged-in user hierarchy. example Genvolt would see all the childs under its hierarchy but Acme Corporation should only see AC1 Corp,AC2 Corp & AC1 Child Corp because its child of AC1 Corp

on client Management, while Add New Client the Parent Client dropdown should only show clients which are below logged-in user hierarchy. example Genvolt would see all the childs under its hierarchy but Acme Corporation should only see AC1 Corp,AC2 Corp & AC1 Child Corp because its child of AC1 Corp